{
    "consumer": {
        "name": "{{ consumer.name }}",
        "id": "{{ consumer.id }}",
        "customId": "{{ consumer.customId }}",
        "groups": {{ consumer.groups }}
    },
    "user": {
        "name": "{{ user.name }}"
    }
}
